<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li style="list-style: none;"><input type="checkbox" <%= task.completed ? 'checked' : '' %>><%= link_to task.title, task_path(task) %>: <%= task.details %></li>
  <% end %>
</ul>

<%= link_to 'Create new task', new_task_path %>
